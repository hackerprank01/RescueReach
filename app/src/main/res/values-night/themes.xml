<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme - Dark Mode -->
    <style name="Theme.RescueReach" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/rescue_primary</item>
        <item name="colorPrimaryVariant">@color/rescue_primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/rescue_accent_light</item>
        <item name="colorSecondaryVariant">@color/rescue_accent</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Text colors -->
        <item name="android:textColorPrimary">@color/text_primary_dark</item>
        <item name="android:textColorSecondary">@color/text_secondary_dark</item>

        <!-- Background colors -->
        <item name="backgroundColor">@color/background_dark</item>
        <item name="cardBackgroundColor">@color/card_background_dark</item>

        <!-- Custom attributes -->
        <item name="gradientBackgroundStart">@color/gradient_start_dark</item>
        <item name="gradientBackgroundEnd">@color/gradient_end_dark</item>
    </style>
</resources>